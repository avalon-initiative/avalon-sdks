name: PR Format Lint

on:
  pull_request:
    types: [opened, edited, synchronize, reopened]

jobs:
  call:
    permissions:
      contents: read
      pull-requests: read
    uses: avalon-initiative/.github/.github/workflows/pr-format-lint.yml@main

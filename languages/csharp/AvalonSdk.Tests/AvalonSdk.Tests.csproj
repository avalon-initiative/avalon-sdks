<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <IsPackable>false</IsPackable>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.NET.Test.Sdk" Version="18.10.1" />
    <PackageReference Include="xunit" Version="2.9.3" />
    <PackageReference Include="xunit.runner.visualstudio" Version="4.0.0" />
    <!-- Live tests only (LiveTests.cs, gated behind AVALON_SERVER_URL/DATABASE_URL) — seeds
         a friendship directly via SQL, mirroring crates/sdk/tests/social.rs's own sqlx use;
         there is no SDK-level way to create one without a friend-request/accept flow. -->
    <PackageReference Include="Npgsql" Version="10.0.3" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\AvalonSdk\AvalonSdk.csproj" />
  </ItemGroup>

</Project>

[package]
name = "avalon-schema-derive"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "#[derive(AvalonSchema)] — generates the .proto message text, and the default_visibility/field_visibility maps, an Integrator Space schema publication (issue #386) needs from an ordinary Rust struct. Not used directly; re-exported through avalon-sdk::schema."

[lib]
proc-macro = true

[dependencies]
syn = { workspace = true, features = ["full"] }
quote.workspace = true
proc-macro2.workspace = true
